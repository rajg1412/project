# the csv data file is in zip mode ( due to large data file ) to use it

# First Unzip it then just keep the csv file in the same folder

# Folder where csv will be present

# backend -> src -> data (here the sales_data.csv file will be there )
